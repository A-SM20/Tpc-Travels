specVersion: "v1beta"
serviceId: "tpc"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "tpc-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
